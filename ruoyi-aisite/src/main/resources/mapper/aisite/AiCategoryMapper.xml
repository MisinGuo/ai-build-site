<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.aisite.mapper.AiCategoryMapper">

    <resultMap type="AiCategory" id="AiCategoryResult">
        <result property="id"          column="id"          />
        <result property="siteId"      column="site_id"     />
        <result property="typeCode"    column="type_code"   />
        <result property="parentId"    column="parent_id"   />
        <result property="name"        column="name"        />
        <result property="slug"        column="slug"        />
        <result property="icon"        column="icon"        />
        <result property="description" column="description" />
        <result property="sortOrder"   column="sort_order"  />
        <result property="itemCount"   column="item_count"  />
        <result property="status"      column="status"      />
        <result property="delFlag"     column="del_flag"    />
        <result property="createBy"    column="create_by"   />
        <result property="createTime"  column="create_time" />
        <result property="updateBy"    column="update_by"   />
        <result property="updateTime"  column="update_time" />
    </resultMap>

    <sql id="selectAiCategoryVo">
        select c.id, c.site_id, c.type_code, c.parent_id, c.name, c.slug, c.icon, c.description,
               c.sort_order, c.item_count, c.status, c.del_flag,
               c.create_by, c.create_time, c.update_by, c.update_time
        from ai_categories c
    </sql>

    <select id="selectAiCategoryById" parameterType="Long" resultMap="AiCategoryResult">
        <include refid="selectAiCategoryVo"/>
        where c.id = #{id} and c.del_flag = '0'
    </select>

    <select id="selectAiCategoryBySlug" resultMap="AiCategoryResult">
        <include refid="selectAiCategoryVo"/>
        where c.slug = #{slug} and c.type_code = #{typeCode} and c.del_flag = '0'
          and (#{siteId} is null or c.site_id = #{siteId} or c.site_id is null)
        limit 1
    </select>

    <select id="selectAiCategoryList" parameterType="AiCategory" resultMap="AiCategoryResult">
        <include refid="selectAiCategoryVo"/>
        <where>
            c.del_flag = '0'
            <if test="siteId != null">
                and (c.site_id = #{siteId} or c.site_id is null)
            </if>
            <if test="typeCode != null and typeCode != ''">
                and c.type_code = #{typeCode}
            </if>
            <if test="parentId != null">
                and c.parent_id = #{parentId}
            </if>
            <if test="name != null and name != ''">
                and c.name like concat('%', #{name}, '%')
            </if>
            <if test="status != null and status != ''">
                and c.status = #{status}
            </if>
        </where>
        order by c.sort_order asc, c.create_time desc
    </select>

    <insert id="insertAiCategory" parameterType="AiCategory" useGeneratedKeys="true" keyProperty="id">
        insert into ai_categories
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="siteId != null">site_id,</if>
            <if test="typeCode != null and typeCode != ''">type_code,</if>
            <if test="parentId != null">parent_id,</if>
            <if test="name != null and name != ''">name,</if>
            <if test="slug != null and slug != ''">slug,</if>
            <if test="icon != null">icon,</if>
            <if test="description != null">description,</if>
            <if test="sortOrder != null">sort_order,</if>
            <if test="status != null">status,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="siteId != null">#{siteId},</if>
            <if test="typeCode != null and typeCode != ''">#{typeCode},</if>
            <if test="parentId != null">#{parentId},</if>
            <if test="name != null and name != ''">#{name},</if>
            <if test="slug != null and slug != ''">#{slug},</if>
            <if test="icon != null">#{icon},</if>
            <if test="description != null">#{description},</if>
            <if test="sortOrder != null">#{sortOrder},</if>
            <if test="status != null">#{status},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>

    <update id="updateAiCategory" parameterType="AiCategory">
        update ai_categories
        <trim prefix="SET" suffixOverrides=",">
            <if test="typeCode != null and typeCode != ''">type_code = #{typeCode},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="slug != null and slug != ''">slug = #{slug},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="description != null">description = #{description},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <update id="deleteAiCategoryById" parameterType="Long">
        update ai_categories set del_flag = '1' where id = #{id}
    </update>

    <update id="deleteAiCategoryByIds" parameterType="Long">
        update ai_categories set del_flag = '1' where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
